const W=16,H=12,C=40;let map=Array.from({length:H},(_,y)=>Array.from({length:W},(_,x)=>x==0||y==0||x==W-1||y==H-1?1:0));
const ed=document.querySelector('#mapEditor'),ec=ed.getContext('2d'),status=document.querySelector('#status');function draw(){ec.fillStyle='#171a21';ec.fillRect(0,0,640,480);for(let y=0;y<H;y++)for(let x=0;x<W;x++){ec.fillStyle=map[y][x]?'#667083':'#20252f';ec.fillRect(x*C+1,y*C+1,C-2,C-2)}}draw();let drawing=false;function paint(e,v){let r=ed.getBoundingClientRect(),x=Math.floor((e.clientX-r.left)*640/r.width/C),y=Math.floor((e.clientY-r.top)*480/r.height/C);if(x>0&&x<W-1&&y>0&&y<H-1){map[y][x]=v;draw()}}ed.onmousedown=e=>{drawing=true;paint(e,e.button===0?1:0)};ed.onmousemove=e=>{if(drawing)paint(e,e.buttons?1:0)};onmouseup=()=>drawing=false;ed.oncontextmenu=e=>e.preventDefault();
const hp=document.querySelector('#hostPanel'),jp=document.querySelector('#joinPanel'),fp=document.querySelector('#finishPanel');hostTab.onclick=()=>{hp.hidden=false;jp.hidden=true};joinTab.onclick=()=>{hp.hidden=true;jp.hidden=false};let pc,dc,host=false,id=null,players={},uid=Math.random().toString(36).slice(2),lastAtk=0;
async function ice(){if(pc.iceGatheringState!='complete')await new Promise(r=>{let f=()=>{if(pc.iceGatheringState=='complete'){pc.removeEventListener('icegatheringstatechange',f);r()}};pc.addEventListener('icegatheringstatechange',f);setTimeout(r,5000)})}function send(o){if(dc?.readyState==='open')dc.send(JSON.stringify(o))}
function channel(c){dc=c;dc.onopen=()=>{status.textContent='Connected. Click the game, then WASD + mouse. Space = stab.';if(!host)send({t:'hello',id:uid});else start()};dc.onmessage=e=>msg(JSON.parse(e.data));dc.onclose=()=>status.textContent='Disconnected'}
async function makeHost(){host=true;id='host';players={host:{id:'host',x:2.5,y:2.5,a:0,alive:true,k:0}};pc=new RTCPeerConnection({iceServers:[{urls:'stun:stun.l.google.com:19302'}]});channel(pc.createDataChannel('game'));let o=await pc.createOffer();await pc.setLocalDescription(o);await ice();offerOut.value=JSON.stringify(pc.localDescription);fp.hidden=false;status.textContent='Send the offer to one player, then paste their answer here.';hostBtn.disabled=true}hostBtn.onclick=makeHost;
connectBtn.onclick=async()=>{try{await pc.setRemoteDescription(JSON.parse(remoteAnswer.value));status.textContent='Connecting...'}catch(e){status.textContent=e.message}};
async function makeJoin(){pc=new RTCPeerConnection({iceServers:[{urls:'stun:stun.l.google.com:19302'}]});pc.ondatachannel=e=>channel(e.channel);try{await pc.setRemoteDescription(JSON.parse(offerIn.value));let a=await pc.createAnswer();await pc.setLocalDescription(a);await ice();answerOut.value=JSON.stringify(pc.localDescription);status.textContent='Send the answer back to the host.';joinBtn.disabled=true}catch(e){status.textContent=e.message}}joinBtn.onclick=makeJoin;
function msg(m){if(host){if(m.t==='hello'&&!players[m.id]){players[m.id]={id:m.id,x:W-3.5,y:H-3.5,a:Math.PI,alive:true,k:0};send({t:'state',map,players,id:m.id})}if(m.t==='input'){let p=players[m.id];if(p){p.x=m.x;p.y=m.y;p.a=m.a;if(m.attack&&performance.now()-lastAtk>350){lastAtk=performance.now();stab(m.id)}}send({t:'snap',players})}}else{if(m.t==='state'){map=m.map;players=m.players;id=m.id;me.x=players[id].x;me.y=players[id].y;me.a=players[id].a;me.alive=true;start()}if(m.t==='snap'){players=m.players;if(players[id])me.alive=players[id].alive}if(m.t==='hit')flash=m.victim===id?'STABBED!':'HIT!'}}}
function stab(att){let a=players[att];if(!a?.alive)return;for(let k in players){let b=players[k];if(k===att||!b.alive)continue;let dx=b.x-a.x,dy=b.y-a.y,d=Math.hypot(dx,dy);if(d>.95)continue;let aim=Math.abs(Math.atan2(Math.sin(Math.atan2(dy,dx)-a.a),Math.cos(Math.atan2(dy,dx)-a.a)));if(aim>.72)continue;let victimToAttacker=Math.atan2(a.y-b.y,a.x-b.x);if(Math.cos(victimToAttacker-b.a)<=0){b.alive=false;a.k++;send({t:'hit',victim:k})}}}
let game=document.querySelector('#game'),g=game.getContext('2d'),me={x:2.5,y:2.5,a:0,alive:true},keys={},running=false,flash='';onkeydown=e=>{keys[e.code]=true;if(e.code==='Space')e.preventDefault()};onkeyup=e=>keys[e.code]=false;document.onclick=()=>{if(!game.hidden)game.requestPointerLock?.()};document.onmousemove=e=>{if(document.pointerLockElement===game)me.a+=e.movementX*.0025};function start(){if(running||!dc||dc.readyState!=='open')return;running=true;document.querySelector('#menu').hidden=true;game.hidden=false;hud.hidden=false;resize();requestAnimationFrame(loop)}function resize(){game.width=Math.min(innerWidth,1280);game.height=Math.min(innerHeight,720)}onresize=resize;
function wall(x,y){return x<0||y<0||x>=W||y>=H||map[Math.floor(y)]?.[Math.floor(x)]===1}function move(dx,dy){if(!wall(me.x+dx,me.y))me.x+=dx;if(!wall(me.x,me.y+dy))me.y+=dy}function ray(a){let sx=Math.cos(a),sy=Math.sin(a),x=me.x,y=me.y,d=0;while(d<30){x+=sx*.035;y+=sy*.035;d+=.035;if(wall(x,y))break}return d}
function render(){let w=game.width,h=game.height;g.fillStyle='#171923';g.fillRect(0,0,w,h/2);g.fillStyle='#29251f';g.fillRect(0,h/2,w,h/2);let f=Math.PI/3,n=Math.ceil(w/3);for(let i=0;i<n;i++){let a=me.a-f/2+f*(i+.5)/n,d=ray(a)*Math.cos(a-me.a),hh=Math.min(h*2,h/Math.max(.01,d));g.fillStyle='#808a99';g.fillRect(i*w/n,h/2-hh/2,w/n+1,hh)}let seen=[];for(let k in players){let p=players[k];if(!p.alive||(!host&&k===id))continue;let dx=p.x-me.x,dy=p.y-me.y,d=Math.hypot(dx,dy),a=Math.atan2(dy,dx)-me.a;a=Math.atan2(Math.sin(a),Math.cos(a));if(Math.abs(a)<f*.65&&ray(me.a+a)>d-.2){let x=w/2+Math.tan(a)/Math.tan(f/2)*w/2,s=Math.min(h*1.5,h/(d*.9));seen.push({x,s,d})}}seen.sort((a,b)=>b.d-a.d);for(let q of seen){g.fillStyle='#ddd';g.fillRect(q.x-q.s*.12,h/2-q.s*.75,q.s*.24,q.s*.7);g.fillStyle='#b9c1cf';g.beginPath();g.arc(q.x,h/2-q.s*.88,q.s*.16,0,7);g.fill()}g.strokeStyle='#fff';g.beginPath();g.moveTo(w/2-8,h/2);g.lineTo(w/2+8,h/2);g.moveTo(w/2,h/2-8);g.lineTo(w/2,h/2+8);g.stroke();if(flash){g.fillStyle='#fff';g.font='bold 32px system-ui';g.textAlign='center';g.fillText(flash,w/2,h*.25);setTimeout(()=>flash='',400)}}
let last=0;function loop(t){let dt=Math.min(.04,(t-last||16)/1000);last=t;if(me.alive){let s=2.7*dt;if(keys.KeyW)move(Math.cos(me.a)*s,Math.sin(me.a)*s);if(keys.KeyS)move(-Math.cos(me.a)*s,-Math.sin(me.a)*s);if(keys.KeyA)move(Math.cos(me.a-Math.PI/2)*s,Math.sin(me.a-Math.PI/2)*s);if(keys.KeyD)move(Math.cos(me.a+Math.PI/2)*s,Math.sin(me.a+Math.PI/2)*s)}if(host){players.host.x=me.x;players.host.y=me.y;players.host.a=me.a;players.host.alive=me.alive;if(keys.Space&&t-lastAtk>350){lastAtk=t;stab('host')}if(dc?.readyState==='open'&&t%50<20)send({t:'snap',players})}else if(dc?.readyState==='open'&&t%50<20)send({t:'input',id,x:me.x,y:me.y,a:me.a,attack:keys.Space});render();playersEl.textContent='Players: '+Object.keys(players).length;state.textContent=me.alive?'Alive — flank them':'ELIMINATED';requestAnimationFrame(loop)}